export enum MajorOrderAssociation {
    unrelated = 0,
    tertiary = 1,
    associated = 2,
    mainObjective = 3
}

export enum MajorOrderType {
    unknown = -1,
    liberate = 0,
    control = 1,
    defend = 2,
    kill = 3
}

export enum EventResult {
    active = 0,
    success = 1,
    failure = 2
}